name: Build and Release Plugin

on:
  push:
    tags:
      - 'plugin-*-v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-24.04
    concurrency:
      group: plugin-release-${{ github.ref_name }}
      cancel-in-progress: true
    steps:
      - name: Parse tag
        shell: pwsh
        run: |
          $tag = "${{ github.ref_name }}"
          # Extract plugin name and version from tag: plugin-sherpa-onnx-v1.0.0
          if ($tag -match '^plugin-(.+)-v(.+)$') {
            $pluginName = $Matches[1]
            $pluginVersion = $Matches[2]
          } else {
            throw "Invalid tag format: $tag"
          }
          echo "PLUGIN_NAME=$pluginName" >> $env:GITHUB_ENV
          echo "PLUGIN_VERSION=$pluginVersion" >> $env:GITHUB_ENV
          echo "Plugin: $pluginName, Version: $pluginVersion"

      - uses: actions/checkout@v7

      - name: Resolve project path
        shell: pwsh
        run: |
          $matrix = & ./scripts/plugin-catalog.ps1 -View ReleaseMatrix | ConvertFrom-Json
          # Not $matches: that is PowerShell's automatic -match capture variable.
          $slugMatches = @($matrix.include | Where-Object { $_.releaseSlug -ceq $env:PLUGIN_NAME })
          if ($slugMatches.Count -ne 1) {
            throw "Unknown plugin: $env:PLUGIN_NAME"
          }
          $entry = $slugMatches[0]
          if (@($entry.platforms).Count -ne 1 -or @($entry.rids).Count -ne 1) {
            throw "Plugin releases currently require exactly one platform and RID."
          }
          echo "PROJECT_NAME=$($entry.projectName)" >> $env:GITHUB_ENV
          echo "PROJECT_PATH=$($entry.projectPath)" >> $env:GITHUB_ENV
          echo "PROJECT_DIRECTORY=$($entry.projectDirectory)" >> $env:GITHUB_ENV
          echo "PLUGIN_PLATFORM=$($entry.platforms[0])" >> $env:GITHUB_ENV
          echo "PLUGIN_RID=$($entry.rids[0])" >> $env:GITHUB_ENV
          echo "PLUGIN_SDK_ABI=$($entry.sdkAbi)" >> $env:GITHUB_ENV
          echo "Project: $($entry.projectPath)"

      - uses: actions/setup-dotnet@v5
        with:
          global-json-file: global.json

      - name: Test plugin system
        timeout-minutes: 15
        run: dotnet test tests/TypeWhisper.PluginSystem.Tests/TypeWhisper.PluginSystem.Tests.csproj -c Release -p:DeployBundledLinuxPlugins=false --blame-hang --blame-hang-timeout 5m --blame-hang-dump-type mini

      - name: Patch manifest version
        shell: pwsh
        run: |
          $manifestPath = Join-Path $env:PROJECT_DIRECTORY 'manifest.json'
          $manifest = Get-Content $manifestPath | ConvertFrom-Json
          $manifest.version = $env:PLUGIN_VERSION
          $manifest | ConvertTo-Json -Depth 4 | Set-Content $manifestPath
          echo "Patched $manifestPath to version $env:PLUGIN_VERSION; the plugin build reads this value directly."
          Get-Content $manifestPath

      - name: Build Plugin
        shell: pwsh
        run: |
          dotnet build $env:PROJECT_PATH -c Release
          if ($LASTEXITCODE -ne 0) { throw "Build failed" }

      - name: Create ZIP
        shell: pwsh
        run: |
          . ./scripts/publish-plugin-release.ps1
          $outputDir = Join-Path $env:PROJECT_DIRECTORY 'bin/Release/net10.0'
          $manifestPath = Join-Path $env:PROJECT_DIRECTORY 'manifest.json'
          $manifest = Get-Content $manifestPath | ConvertFrom-Json
          $projection = Get-PluginRegistryProjection -Manifest $manifest
          $pluginId = $manifest.id
          $assemblyName = $manifest.assemblyName -replace '\.dll$',''

          # Create staging directory
          $stagingDir = "plugin-staging/$pluginId"
          New-Item -ItemType Directory -Path $stagingDir -Force | Out-Null

          # Copy essential files
          Copy-Item (Join-Path $outputDir "$assemblyName.dll") $stagingDir
          $depsFile = Join-Path $outputDir "$assemblyName.deps.json"
          if (Test-Path $depsFile) { Copy-Item $depsFile $stagingDir }
          foreach ($property in $projection.PSObject.Properties) {
            $manifest | Add-Member -NotePropertyName $property.Name -NotePropertyValue $property.Value -Force
          }
          $manifest | ConvertTo-Json -Depth 20 |
            Set-Content (Join-Path $stagingDir 'manifest.json') -Encoding utf8NoBOM

          # Copy Localization folder if present
          $locDir = Join-Path $env:PROJECT_DIRECTORY 'Localization'
          if (Test-Path $locDir) {
            Copy-Item -Path $locDir -Destination "$stagingDir/Localization" -Recurse
          }

          # Copy plugin Scripts folder if present. Some local-model plugins need
          # these runtime files (e.g. Python) after the ZIP is installed from the registry.
          $scriptsDir = Join-Path $env:PROJECT_DIRECTORY 'Scripts'
          if (Test-Path $scriptsDir) {
            Copy-Item -Path $scriptsDir -Destination "$stagingDir/Scripts" -Recurse
            Write-Host "  Included plugin scripts"
          }

          # Copy NuGet dependency DLLs (excluding SDK/framework assemblies)
          $excludeAssemblies = @('TypeWhisper.PluginSDK.dll')
          Get-ChildItem -Path $outputDir -Filter '*.dll' | Where-Object {
            $_.Name -ne "$assemblyName.dll" -and $_.Name -notin $excludeAssemblies
          } | ForEach-Object {
            Copy-Item $_.FullName $stagingDir
            Write-Host "  Included dependency: $($_.Name)"
          }

          # Copy only the runtime declared by the catalog/release entry.
          $runtimeRidDir = Join-Path $outputDir "runtimes/$env:PLUGIN_RID"
          if (Test-Path $runtimeRidDir) {
            $runtimeStagingDir = "$stagingDir/runtimes/$env:PLUGIN_RID"
            New-Item -ItemType Directory -Path (Split-Path $runtimeStagingDir -Parent) -Force | Out-Null
            Copy-Item -Path $runtimeRidDir -Destination $runtimeStagingDir -Recurse
            Write-Host "  Included native runtime: $env:PLUGIN_RID"
          }

          # Fail closed: if the plugin ships a Scripts/ dir but it didn't reach
          # staging, abort rather than publish an incomplete package.
          if ((Test-Path $scriptsDir) -and -not (Test-Path "$stagingDir/Scripts")) {
            throw "Plugin scripts were not copied to staging: $scriptsDir"
          }

          # Create ZIP
          $zipName = "$pluginId-$env:PLUGIN_VERSION.zip"
          Compress-Archive -Path "$stagingDir/*" -DestinationPath $zipName -Force
          $zipSize = (Get-Item $zipName).Length
          echo "ZIP_NAME=$zipName" >> $env:GITHUB_ENV
          echo "ZIP_SIZE=$zipSize" >> $env:GITHUB_ENV
          echo "PLUGIN_ID=$pluginId" >> $env:GITHUB_ENV
          echo "Created $zipName ($zipSize bytes)"

      - name: Publish plugin release transaction
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        shell: pwsh
        run: |
          ./scripts/publish-plugin-release.ps1 `
            -Tag $env:GITHUB_REF_NAME `
            -Repository $env:GITHUB_REPOSITORY `
            -CommitSha $env:GITHUB_SHA `
            -ProjectName $env:PROJECT_NAME `
            -PluginVersion $env:PLUGIN_VERSION `
            -PluginId $env:PLUGIN_ID `
            -Platform $env:PLUGIN_PLATFORM `
            -Rid $env:PLUGIN_RID `
            -SdkAbi $env:PLUGIN_SDK_ABI `
            -ZipPath $env:ZIP_NAME `
            -ManifestPath (Join-Path $env:PROJECT_DIRECTORY 'manifest.json')
