name: Plugins Smoke

on:
  pull_request:
    branches:
      - linux
  push:
    branches:
      - linux
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: plugins-smoke-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  discover-plugins:
    runs-on: ubuntu-latest
    outputs:
      projects: ${{ steps.discover.outputs.projects }}
      has_projects: ${{ steps.discover.outputs.has_projects }}
      scan_mode: ${{ steps.discover.outputs.scan_mode }}

    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Run plugin release transaction regression
        shell: pwsh
        run: |
          $minimumPesterVersion = [version]'5.5.0'
          $pester = Get-Module -ListAvailable Pester |
            Where-Object Version -ge $minimumPesterVersion |
            Sort-Object Version -Descending |
            Select-Object -First 1
          if (-not $pester) {
            Install-Module Pester -RequiredVersion 5.7.1 -Scope CurrentUser -Force
            $pester = Get-Module -ListAvailable Pester |
              Where-Object Version -ge $minimumPesterVersion |
              Sort-Object Version -Descending |
              Select-Object -First 1
          }
          Import-Module $pester.Path -Force

          $result = Invoke-Pester `
            -Path tests/workflows/PublishPluginReleaseTransaction.Tests.ps1 `
            -Output Detailed `
            -PassThru
          if ($result.FailedCount -gt 0) {
            throw "$($result.FailedCount) plugin release transaction test(s) failed."
          }

      - name: Test plugin smoke discovery
        shell: pwsh
        run: pwsh -NoProfile -File tests/workflows/PluginSmokeDiscovery.Tests.ps1

      - id: discover
        shell: pwsh
        run: |
          $changedFiles = @()
          if ($env:GITHUB_EVENT_NAME -eq 'pull_request') {
            $baseSha = '${{ github.event.pull_request.base.sha }}'
            # --no-renames is load-bearing: with rename detection on, moving a
            # file out of plugins/Shared reports only the destination path, so
            # the shared-code trigger never fires and the other plugins that
            # <Compile Include> that file are never smoke-built. Splitting
            # renames into delete+add can only widen the matrix, never narrow it.
            $changedFiles = @(git diff --name-only --no-renames $baseSha HEAD)
          }

          $result = & "$env:GITHUB_WORKSPACE/scripts/select-plugin-smoke-projects.ps1" `
            -ChangedFiles $changedFiles `
            -EventName $env:GITHUB_EVENT_NAME `
            -WorkspaceRoot $env:GITHUB_WORKSPACE

          "projects=$($result.Projects)" >> $env:GITHUB_OUTPUT
          "has_projects=$($result.HasProjects)" >> $env:GITHUB_OUTPUT
          "scan_mode=$($result.ScanMode)" >> $env:GITHUB_OUTPUT

  validate-metadata:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Validate plugin manifests and localization files
        shell: pwsh
        run: |
          $errors = [System.Collections.Generic.List[string]]::new()

          foreach ($manifestPath in Get-ChildItem plugins -Recurse -Filter manifest.json | Sort-Object FullName) {
            try {
              $manifest = Get-Content $manifestPath.FullName -Raw | ConvertFrom-Json
            } catch {
              $errors.Add("Invalid JSON in $($manifestPath.FullName): $($_.Exception.Message)")
              continue
            }

            foreach ($key in 'id', 'name', 'version', 'assemblyName', 'pluginClass') {
              if ([string]::IsNullOrWhiteSpace([string]$manifest.$key)) {
                $errors.Add("Missing or empty '$key' in $($manifestPath.FullName)")
              }
            }

            if ($manifest.assemblyName -and -not ([string]$manifest.assemblyName).EndsWith('.dll')) {
              $errors.Add("assemblyName must end with .dll in $($manifestPath.FullName)")
            }

            $localizationDir = Join-Path $manifestPath.Directory.FullName 'Localization'
            if (Test-Path $localizationDir) {
              foreach ($locPath in Get-ChildItem $localizationDir -Filter *.json | Sort-Object FullName) {
                try {
                  $localization = Get-Content $locPath.FullName -Raw | ConvertFrom-Json -AsHashtable
                } catch {
                  $errors.Add("Invalid JSON in $($locPath.FullName): $($_.Exception.Message)")
                  continue
                }

                if ($localization -isnot [System.Collections.IDictionary]) {
                  $errors.Add("Localization file must contain a JSON object: $($locPath.FullName)")
                }
              }
            }
          }

          if ($errors.Count -gt 0) {
            $errors | ForEach-Object { Write-Error $_ }
            exit 1
          }

  build-plugin:
    needs: discover-plugins
    if: needs.discover-plugins.outputs.has_projects == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        plugin_path: ${{ fromJson(needs.discover-plugins.outputs.projects) }}

    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Resolve plugin metadata
        id: meta
        shell: pwsh
        env:
          PLUGIN_PATH: ${{ matrix.plugin_path }}
        run: |
          if ([string]::IsNullOrWhiteSpace($env:PLUGIN_PATH)) {
            throw "Matrix value PLUGIN_PATH is empty; discover-plugins emitted a bad entry."
          }
          $name = [System.IO.Path]::GetFileNameWithoutExtension($env:PLUGIN_PATH)
          "name=$name" >> $env:GITHUB_OUTPUT
          Write-Host "Plugin: $name ($env:PLUGIN_PATH)"

      - name: Ensure NuGet global packages folder
        shell: pwsh
        run: New-Item -ItemType Directory -Force -Path (Join-Path $env:HOME '.nuget/packages') | Out-Null

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'
          cache: true
          cache-dependency-path: |
            **/*.csproj
            Directory.Build.props

      - name: Create artifact directory
        shell: pwsh
        run: New-Item -ItemType Directory -Force -Path artifacts/logs | Out-Null

      - name: Restore ${{ steps.meta.outputs.name }}
        env:
          PLUGIN_PATH: ${{ matrix.plugin_path }}
        run: dotnet restore "$PLUGIN_PATH"

      - name: Build ${{ steps.meta.outputs.name }}
        env:
          PLUGIN_PATH: ${{ matrix.plugin_path }}
          PLUGIN_NAME: ${{ steps.meta.outputs.name }}
        run: dotnet build "$PLUGIN_PATH" -c Release --no-restore -bl:"artifacts/logs/${PLUGIN_NAME}.binlog"

      - name: Validate built output
        shell: pwsh
        env:
          PLUGIN_PATH: ${{ matrix.plugin_path }}
        run: |
          $projectPath = Resolve-Path $env:PLUGIN_PATH
          $projectDir = Split-Path $projectPath -Parent
          $manifestPath = Join-Path $projectDir 'manifest.json'
          $manifest = Get-Content $manifestPath -Raw | ConvertFrom-Json
          $outputDir = Join-Path $projectDir 'bin/Release/net10.0'
          $assemblyPath = Join-Path $outputDir $manifest.assemblyName
          $copiedManifestPath = Join-Path $outputDir 'manifest.json'

          if (-not (Test-Path $assemblyPath)) {
            throw "Expected plugin assembly not found: $assemblyPath"
          }

          if (-not (Test-Path $copiedManifestPath)) {
            throw "manifest.json was not copied to output: $copiedManifestPath"
          }

          $localizationSourceDir = Join-Path $projectDir 'Localization'
          if (Test-Path $localizationSourceDir) {
            $builtLocalizationDir = Join-Path $outputDir 'Localization'
            if (-not (Test-Path $builtLocalizationDir)) {
              throw "Built localization directory missing: $builtLocalizationDir"
            }
          }

          $scriptSourceDir = Join-Path $projectDir 'Scripts'
          if (Test-Path $scriptSourceDir) {
            $builtScriptDir = Join-Path $outputDir 'Scripts'
            if (-not (Test-Path $builtScriptDir)) {
              throw "Built scripts directory missing: $builtScriptDir"
            }
          }

      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: plugins-smoke-${{ steps.meta.outputs.name }}
          path: artifacts/logs/*.binlog
          if-no-files-found: ignore
